---
title: "DoubletFinder (sample)"
output: html_document
author: "Kevin Rue"
date: "`r Sys.Date()`"
---

```{r libraries, include=FALSE}
library(DoubletFinder)
```

```{r constants, include=FALSE}
umi_breaks <- 10^(-1:10)
umi_minor_breaks <- rep(c(1, 2, 5), 12) * (10^rep(-1:10, each = 3))
barcode_breaks <- 10^(-1:10)
barcode_minor_breaks <- 10^rep(-1:10)
```

```{r snakemake, include=FALSE}
if (interactive()) {
  rds_dbl <- "results/doubletfinder/result/WPPp000hrs_rep2.rds"
} else {
  rds_dbl <- snakemake@input[["doubletfinder"]]
}
sample_name <- fs::path_ext_remove(basename(rds_dbl))
```

## Session info

`r date()`

```{r session_info, echo=FALSE}
sessionInfo()
```
